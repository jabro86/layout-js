// Import here Polyfills if needed. Recommended core-js (npm i -D core-js)
// import "core-js/fn/array.find"
// ...

/**
 * Function send greetings to the given name
 *
 * @param name
 */
export function greeter(name: string): string {
  return "hello " + name;
}
